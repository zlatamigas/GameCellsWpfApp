namespace QueenGame.Game.Models
{
    public enum CellState
    {
        NOT_SELECTED = 0,
        SELECTED = 1,
        UNAVAILABLE = 2,
        MARKED = 3,
        // SELECTED_CHANGE_FORBIDDEN = 4
    }
}
